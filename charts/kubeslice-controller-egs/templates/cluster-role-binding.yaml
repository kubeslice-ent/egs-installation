{{- if .Values.rbac.create }}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "kubeslice-controller.fullname" . }}
  labels:
    {{- include "kubeslice-controller.labels" . | nindent 4 }}
    app.kubernetes.io/component: rbac
  {{- with .Values.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
subjects:
  - kind: ServiceAccount
    name: {{ include "kubeslice-controller.serviceAccountName" . }}
    namespace: {{ include "kubeslice-controller.namespace" .}}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "kubeslice-controller.fullname" . }}
{{- end }}